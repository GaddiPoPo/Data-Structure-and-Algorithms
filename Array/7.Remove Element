class Solution(object):
    def removeElement(self, nums, val):
        k=0
        n=len(nums)
        for i in range(n):
            if nums[i] !=val:
                nums[k]=nums[i]
                k+=1
        return k

